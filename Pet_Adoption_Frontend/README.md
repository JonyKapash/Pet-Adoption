This project is a fully functional pet adoption website build with react as the main framework, node.js with express as the server and SQL database.

In the project directory, you can run:

### `npm start`

